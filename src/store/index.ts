import Vue from 'vue'
import Vuex from 'vuex'
import goodsStore from "@/store/main-store/main-store";

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    goodsStore,
  }
})
